faculty_members = ['John', 'Alice', 'ProfessorX', 'Maximilian', 'Sarah', 'William', 'Jonathan']
filtered_names = [name for name in faculty_members if len(name) > 8]
print("Names with more than 8 characters:", filtered_names)
